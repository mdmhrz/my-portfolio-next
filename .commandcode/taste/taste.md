# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# Package Management
- Use pnpm for package management. Confidence: 0.65

# Favicon / Icon
- Favicon SVG: Use white logo (#fff) on a dark background (#000) with rounded rect backdrop for browser tab visibility. Confidence: 0.65

# UI Patterns
- For theme toggles: Use direct on-click toggle instead of dropdown menus. Confidence: 0.70

# Theme / Styling
- Use Tailwind CSS theme tokens (text-foreground, bg-background, bg-muted, border-border, text-muted-foreground, bg-card, text-primary-foreground) instead of hardcoded color values (e.g., text-white, bg-black, bg-white/5, border-white/10). Confidence: 0.85

# Hero / Banner
- For hero/banner sections: Make text non-selectable (user-select: none). Confidence: 0.80
